// plugins/reflect-metadata.ts
import 'reflect-metadata';

export default defineNuxtPlugin(() => {
  // Since the import statement is already executed, no additional setup is required in this function.
});
